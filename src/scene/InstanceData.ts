export interface InstanceData {
  customData0: [number, number, number, number];
  customData1: [number, number, number, number];
}

export const DEFAULT_INSTANCE_DATA: InstanceData = {
  customData0: [0, 0, 0, 0],
  customData1: [1, 1, 1, 1],
};
